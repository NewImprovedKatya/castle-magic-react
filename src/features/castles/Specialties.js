import { Row, Col, Badge } from "reactstrap";

const Specialties = () => {
  return (
    <Row className="mb-5 border-top border">
        <Col>
          <Badge color="primary">New</Badge>
        </Col>
        <Col>
          <Badge color="primary">New</Badge>
        </Col>
    </Row>
  );
};

export default Specialties;
